---
description: ExpatTH shared 文档与规则入口
globs: "**/*"
alwaysApply: true
---

# shared 入口规则

每次任务必须先读 `../../CHARTER.md` 与 `../../MANIFEST.md`。

`ep-shared` 只负责合同、术语、状态、数据库计划、规则和审计记录，不放运行时代码。

规则正文以 `../../CONSTITUTION.md` 为准。
