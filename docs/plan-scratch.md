# 规划草稿（临时）

在 Agent 做「Plan」阶段时，可把本文件当作**一次性规划稿**：写清目标、文件路径、验收步骤；确认后再让 Agent 改代码。合并进正式文档或任务完成后可删除本文件内容，避免与 `api-user-list.md` / `api-merchant-list.md` 等正式文档长期重复。

---

（以下为模板示例，可整段替换）

## 目标

## 涉及路径

## 步骤

## 验证
